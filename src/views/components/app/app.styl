#app
  absolute top 0 left 0 bottom 0 right 0

.load__container
  display flex
  flex-direction column
  justify-content center
  align-items center
  width 100%
  height 100%

.tint__icon
  background black
  font-size 5em
  padding 0 1em
